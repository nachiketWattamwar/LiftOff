<head>
  <% include ../partials/head %>

  <script>
    function myfunc() {
      alert("Alert");
    }
  </script>
</head>

<body>
  <!-- <div class="container">
    <div class="md-form mt-0">
      <input
        class="form-control"
        type="text"
        placeholder="Search"
        aria-label="Search"
      />
      <button type="button" class="btn btn-success">Add</button>
    </div>
    <% for(var i=0; i < data.length; i++) { %>
    <div class="row">
      <div class="col-md-12 sapce-project">
        <div class="row">
          <div class="col-md-8" id="imageID">
            <h3 class="space-title">
              <%= data[i].name %>
            </h3>
            <p contenteditable="true" class="space-desc">
              <%= data[i].descp %>
            </p>
            <button type="button" class="btn btn-primary">Edit</button>
            <button type="button" class="btn btn-danger">Delete</button>
          </div>
        </div>
      </div>
      <div class="separator"></div>
      <% } %>
    </div>
  </div> -->
  <!-- <table class="table table-striped">
    <thead>
      <tr>
        <th scope="col">Name</th>
        <th scope="col">Descp</th>
      </tr>
    </thead>
    <tbody> -->
      <% for(var i=0; i < data.length; i++) { %>

        <form name="SignUp" onsubmit="return validateSignUp()" class="form-horizontal" action="/auth/signup" method="GET">
            <div class="form-group">
                <label class="control-label col-sm-2" for="name"
                >Name:</label>
              <div class="col-sm-10">
                <input
                  name="name"
                  type="text"
                  class="form-control"
                  id="name"

                  value="<%= data[i].name %>"
                />
              </div>
              <label class="control-label col-sm-2" for="email"
                >descp:</label>
              <div class="col-sm-10">
                <input
                  name="email"
                  type="text"
                  class="form-control"
                  id="email"
                  value="<%= data[i].descp %>"
                />
              </div>

              <div class="col-sm-offset-2 col-sm-10">
                <button type="submit" class="btn btn-default">Edit</button>
              </div>
            </div>
          </form>
      <!-- <tr>
        <td contenteditable="true"><%= data[i].name %></td>
        <td><%= data[i].descp %></td>
        <td>
          <button type="button" class="btn btn-primary" onclick="myfunc()">
            Edit
          </button>
        </td>
        <td><button type="button" class="btn btn-danger">Delete</button></td>
      </tr> -->
      <% } %>
    </tbody>
  </table>
</body>
